import 'package:flutter/material.dart';

class MindLoadingWidget extends StatelessWidget {
  final double? width;
  final double? height;
  const MindLoadingWidget([this.width = 100, this.height = 100]);

  @override
  Widget build(BuildContext context) {
    return Container();
    // return Image.asset('assets/gif/studenda_loading.apng',width: width, height: height,);
  }
}
